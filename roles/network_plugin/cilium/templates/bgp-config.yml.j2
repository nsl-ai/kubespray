---
apiVersion: v1
kind: ConfigMap
metadata:
  name: bgp-config
  namespace: kube-system
data:
  config.yaml: |-
    peers:
      - peer-address: "{{ cilium_bgp_peer }}"
        peer-asn: 64512
        my-asn: 64512